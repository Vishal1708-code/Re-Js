//let score="123ab";if we change to number if we see console.log and
//  see type of it will give (number) but if we directly console.log it will give (nan).

//let score = null(object) if we change to number if we see console.log and
//  see type of it will give (number) but if we directly console.log it will give (0).

//let score = undefined() if we change to number if we see console.log and
//  see type of it will give (number) but if we directly console.log it will give (nan).

// console.log(typeof score)
// console.log(typeof (score))

// let numberChange = Number(undefined)
// console.log(typeof numberChange)
// console.log(numberChange)

// 33 >= 33
// "123abc" >= NaN
// true >= 1 or false >= 0


// 1 => true
// "hitesh" => true
// " " => false

// let someString = 33
// let convertString = String(someString)
// console.log(convertString)
// console.log(typeof convertString)

//********************* Operations **************************

console.log("ab")